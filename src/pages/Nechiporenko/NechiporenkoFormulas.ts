export const n16 = (x: number): string => (x * 16 * 0.031).toFixed(3)
export const n4 = (x: number): string => (x * 4 * 0.031).toFixed(3)
export const n2 = (x: number): string => (x * 2 * 0.031).toFixed(3)
export const n1 = (x: number): string => (x * 1 * 0.031).toFixed(3)


export const a16 = (x: number): string => ((x * 16) / 3).toFixed(3)
export const a4 = (x: number): string => ((x * 4) / 3).toFixed(3)
export const a2 = (x: number): string => ((x * 2) / 3).toFixed(3)
export const a1 = (x: number): string => ((x * 1) / 3).toFixed(3)

